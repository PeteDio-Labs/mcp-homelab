# Nexus npm registry — resolves @petedio/* scoped packages
# Credentials are injected at build time via --secret, not stored here
@petedio:registry=https://registry.toastedbytes.com/repository/npm-group/
